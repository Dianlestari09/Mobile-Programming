name: ibadahtracker_app
description: Aplikasi tracker ibadah

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.0
  image_picker: ^1.0.4
  table_calendar: ^3.0.9
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # HAPUS BAGIAN FONTS INI